import 'server-only';

export enum PackageConfig {
    version = 'v1alpha1',
    group = 'fs.functionstream.github.io',
    plural = 'packages'
}

export enum FunctionConfig {
    version = 'v1alpha1',
    group = 'fs.functionstream.github.io',
    plural = 'functions'
}

export enum AgentConfig {
    version = 'v1alpha1',
    group = 'as.agentstream.github.io',
    plural = 'agents'
}
