'use client';

import { Space } from 'antd';

const NavBar = () => {
  return (
    <Space className="w-full h-full bg-blue-500 text-center justify-center" align="center">
      topbar
    </Space>
  );
};
export default NavBar;
